// Dashboard feature barrel exports
export { UserDashboard } from './components/UserDashboard';
export { AdminDashboard } from './components/AdminDashboard';
export { DashboardFeatureWrapper } from './components/DashboardFeatureWrapper';

// Export dashboard sections
export {
  BrandingVisualsSection,
  StoreOverviewSection,
  StoreSetupProgress,
} from './components/sections';

// Export routes
export { dashboardRoutes, default as dashboardRoutesDefault } from './routes';

// Export services (when available)
// export * from './services';

// Export types (when available)
// export * from './types';
